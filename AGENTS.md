# SolidGround agent instructions

`AGENTS.md` is the only canonical instruction file in this repository. The agent must treat any tool-specific file as a pointer to this file, never as a second instruction source. The agent must keep the repository usable from T3 Code with Claude Code, Codex, Cursor, Grok Build, and OpenCode. The agent must not add tool-specific servers, commands, hooks, automation, or `.claude/` and `.codex/` directories.

## Mission and current boundary

SolidGround is a Revit 2027 add-in that obtains 1-meter USGS bare-earth elevation data through OpenTopography, clips it to one parcel, simplifies it without erasing important terrain form, and creates a native Revit toposolid with reversible provenance.

Phase 0 establishes the repository. Phase 1 implements `SolidGround.Core`, `SolidGround.Cli`, and offline tests. Phase 2 implements the Revit add-in. The agent must not begin Phase 1 without an explicit implementation task. Phase 1 was signed off on 2026-09-20 (Issue #10). The Revit add-in conventions were approved on 2026-09-20 (Issue #12) and are recorded in `docs/architecture/revit-add-in-conventions.md`. Issue #14 scaffolded `SolidGround.Revit` on 2026-09-21 (manifest, ribbon, a read-only Preflight-and-report command, diagnostics, a deploy script, and the CI compile gate), recorded in `docs/architecture/revit-add-in-host-scaffold.md`. Issue #15 implemented toposolid creation on 2026-09-21: settings-driven `fetch`/`process` acquisition (Revit `process` mode fully supports the optional source sidecar, matching the CLI's own `--source-json`, with no scope reduction), the Core lift of the terrain processing pipeline the CLI now shares with `SolidGround.Revit`, boundary and unit conversion, the transaction-scoped creation service with post-create verification and the shared-coordinate orphan check, and the placement-record writer; recorded in `docs/architecture/revit-toposolid-creation.md`. A 2026-09-21 probe session recorded evidence for that note's manual evidence plan Steps 5, 7, and 8a (Result-code Undo-stack behavior, the point-count threshold, unit factor, invalid-point handling, boundary Z, and the orphan check). Three further 2026-09-21 end-to-end Revit 2027 sessions then recorded Step 8b's own evidence: the shipped add-in's Scenarios A-D, and the `Revit.ini` point-budget guard both before and after it landed, are confirmed against a real Revit 2027 process with output byte-identical to the offline CLI reference; the live OpenTopography fetch through Revit's own process also succeeded, on a re-entered `OPENTOPOGRAPHY_API_KEY` after an earlier session's key value was rejected by OpenTopography's server and reproduced identically by the CLI, creating element id 317345 (Level 1, Toposolid 1, 2810 of 2810 points retained, horizontal reference EPSG:26915, vertical reference NAVD88). Issue #15's implementation and its Revit 2027 evidence, including the live OpenTopography fetch, are complete as of 2026-09-21. Issue #16 attached Extensible Storage provenance to the created toposolid on 2026-09-21 to 2026-09-23 (commits `e63996b` through `0e6c2dd` plus this closeout commit): schema `SolidGround_Provenance_Toposolid` (GUID `bc03d923-8c8a-4a1e-bd2a-8e41f0a4ff6e`), version 1, 36 fields, `AccessLevel.Public` read / `AccessLevel.Vendor` write, recorded in `docs/architecture/revit-extensible-storage-provenance.md`. Issue #19 replaced the placeholder ribbon icons with the final 16x16/32x32 terrain-and-parcel pair on 2026-09-23 to 2026-09-24 (commit `e8eb33f` plus this closeout commit), recorded in `docs/architecture/revit-ribbon-icons.md`, with the resource convention unchanged and a 2026-09-24 Revit 2027 session confirming pixel-exact 1:1 display in the light and dark themes. The agent must follow the conventions note for any further `SolidGround.Revit` work. Issue #17 (begun 2026-09-24) is packaging, signing, and validating the Revit 2027 release: `Directory.Build.props` now declares `<Version>0.1.0</Version>`, `scripts/Sign-RevitAddIn.ps1`/`scripts/Import-SigningTrust.ps1` implement the self-signed Authenticode signing decision, `scripts/New-ReleasePackage.ps1` builds a signed, hashed release zip into `artifacts/release/`, and `scripts/Install-SolidGround.ps1`/`scripts/install.cmd`/`scripts/Uninstall-SolidGround.ps1` install and remove it; recorded in `docs/architecture/revit-release-packaging-and-signing.md`, whose manual evidence plan has not yet run. Phase 3 adds an interactive Revit 2027 add-in workflow that turns a street address into geocoded coordinates, resolves a matching parcel boundary, and lets the operator confirm both — together with an optional shared-coordinates write and a native property line — before today's terrain pipeline runs, as researched in `docs/architecture/phase-3-interactive-add-in-research.md` (2026-09-21). The agent must not begin Phase 3 without an explicit implementation task.

## Architecture

| Project | Target | Responsibility |
| --- | --- | --- |
| `src/SolidGround.Core` | `net10.0` | Revit-free domain logic: AOIs, OpenTopography requests, AAIGrid parsing, coordinate transforms, clipping, unit conversion, local-origin transforms, decimation, provenance, and exports. |
| `src/SolidGround.Cli` | `net10.0` | Thin development and batch interface over Core. |
| `tests/SolidGround.Tests` | `net10.0` | xUnit tests against Core using committed offline fixtures. |
| `src/SolidGround.Revit` | `net10.0-windows` in Phase 2 | Thin Revit host adapter, ribbon application, external command, boundary conversion, toposolid creation, and provenance attachment. |

The agent must keep every Revit reference out of Core, CLI, and Tests. The agent must keep source acquisition pluggable so a future classified point-cloud source can coexist with the initial gridded DEM source. The agent must not use Python, GDAL, native geospatial binaries, or the Forma Connected Client.

Groundit is an architecture reference only: its useful pattern is a host-independent core with offline tests and a thin Revit build step. The agent must not copy its Python, pyRevit, browser, data-source, or multi-version design.

## Revit 2027 rules

The agent must target Revit 2027 and .NET 10 only. The agent must not add Revit 2024, 2025, or 2026 compatibility. Before writing or changing a Revit API call, manifest feature, installation path, or Revit-specific setting, the agent must verify it against the Revit 2027 SDK or Autodesk's 2027 API documentation and cite the source in the relevant design note or pull request. Earlier-version examples are not sufficient evidence.

The local reference installation inspected during setup is Revit 2027 API version `27.0.10.13`. Autodesk's 2027 API changes document confirms the .NET 10 migration and the all-user add-in move from `ProgramData` to `Program Files`. The agent must never hardcode an all-user add-in directory. The add-in should install per-user unless the later installer design requires all-user installation; runtime code that needs the all-user path must read `ControlledApplication.AllUsersAddinsLocation` in `IExternalApplication.OnStartup`/`OnShutdown` (reached as `application.ControlledApplication.AllUsersAddinsLocation`) or `Application.AllUsersAddinsLocation` in command-time code (reached as `commandData.Application.Application.AllUsersAddinsLocation`); both are instance, get-only, `string` properties verified against Revit 2027 (Issue #13, item 4).

Revit 2027 add-in isolation is real and must be used deliberately in Phase 2. The installed `RevitAddInUtility.dll` exposes:

- `Autodesk.RevitAddIns.AddInDependencyBase.AssemblyNames`
- `Autodesk.RevitAddIns.ClientIdDependency`
- `Autodesk.RevitAddIns.ContextNameDependency`
- `Autodesk.RevitAddIns.RevitAddInManifestSettings.UseRevitContext`
- `Autodesk.RevitAddIns.RevitAddInManifestSettings.ContextName`
- `Autodesk.RevitAddIns.RevitAddInManifestSettings.UseAllContextsForDependencyResolution`
- `Autodesk.RevitAddIns.RevitAddInManifestSettings.PublicAssemblies`
- `Autodesk.RevitAddIns.RevitAddInManifestSettings.Dependencies`

The Phase 2 default is an isolated add-in context: set `UseRevitContext` to `false`, use a stable SolidGround context name, leave all-context dependency resolution disabled, and expose or consume no shared assemblies unless a reviewed integration explicitly requires it. The agent must bundle only managed dependencies and keep the geospatial dependency graph private to that context.

The installed Revit 2027 `SiteDB.dll` contains both `NativeToposolidMaxPointThreshold` and `LinkToposolidMaxPointThreshold`, confirming that the settings still exist in 2027. Autodesk's current Revit 2027 product help restates a 10,000 to 50,000 range and a 20,000 default for both settings on its "Misc Settings in Revit.ini", "About Toposolids", and "Link Topography" pages (Issue #13, item 9); no documentation ties either setting to the `Toposolid.Create` API path, and the installed product's own values are read from `Revit.ini` after the first Revit 2027 launch (manual test step 7 in `docs/architecture/revit-2027-verification-and-host-design.md`). The agent must retain a conservative application default near 15,000, make the budget configurable, and re-verify the numeric limits before Phase 2 behavior depends on them. A 2026-09-21 Issue #15 probe session found that Revit's combined `Toposolid.Create(profiles, points, ...)` overload accepts 19,999, 20,000, and 20,001 points with no observable gating tied to either `Revit.ini` setting; a 30,000/50,001 sweep submitted that many genuinely distinct points (the probe's point generator was checked directly, and confirmed by simulation to produce no duplicate coordinates at any tested count) but the resulting toposolid retained only about 20,015 and 20,031 vertices, with no exception thrown either time. With duplicate/degenerate probe input ruled out by that simulation, the evidence-backed interpretation is that Revit itself silently caps the combined overload's retained vertex count near the running machine's own configured `NativeToposolidMaxPointThreshold` once the truly-distinct point count exceeds it, rather than throwing (`docs/architecture/revit-toposolid-creation.md`'s "Step 7"). The agent must keep `CreateToposolidCommand`'s Document Preflight stage reading the machine's real `Revit.ini` before acquisition and before any transaction (`Autodesk.Revit.ApplicationServices.Application.CurrentUsersDataFolderPath`, reached in command-time code as `commandData.Application.Application.CurrentUsersDataFolderPath`; parsed by the Revit-free `SolidGround.Core.Hosting.RevitIniToposolidThresholds`), logging both configured thresholds, and rejecting a configured `pointBudget` that exceeds the machine's own `NativeToposolidMaxPointThreshold` before that transaction opens; a `PostCreationVerification` vertex-count shortfall found anyway must still roll back, with a message naming this setting as the likely cause only when the supplied point count actually exceeded it (Preflight's own guard means a known threshold is normally already respected by that point, so the message says so instead of repeating a remedy that would do nothing). This finding and guard do not change the conservative ~15,000 application default named above.

## Revit add-in conventions

Issue #12 approved the Phase 2 conventions below on 2026-09-20; `docs/architecture/revit-add-in-conventions.md` records the full design basis in the owner's other Revit add-in, citations, and the 14 items still needing Revit 2027 verification (Issue #13). Issue #13 verified all 14 of those items on 2026-09-20, with item 2's Undo-stack side effect and several other items' residual runtime-only questions deferred to the manual integration test plan; the results, the locked thin-host design, and that test plan are recorded in `docs/architecture/revit-2027-verification-and-host-design.md`. Issue #15 implemented toposolid creation on 2026-09-21 following these conventions; its design decisions, settings-file template, placement-record schema, error catalogue, Result-code policy, the boundary-Z decision, the Issue #16 extension point, and the manual evidence plan for the verification note's remaining items 2, 9, 15, and 16 are recorded in `docs/architecture/revit-toposolid-creation.md`, whose manual evidence plan now carries 2026-09-21 probe-session evidence for Steps 5, 7, and 8a plus 2026-09-21 end-to-end Revit 2027 session evidence for Step 8b's own Scenarios A-D, the `Revit.ini` point-budget guard, and the live OpenTopography fetch itself (a re-entered key succeeded on Run 5, creating element id 317345 with 2810 of 2810 points retained); Issue #15's Revit 2027 evidence is complete as of 2026-09-21. Issue #16 attached Extensible Storage provenance to the created toposolid on 2026-09-21 to 2026-09-23 (commits `e63996b` through `0e6c2dd` plus this closeout commit) following these conventions; its Core field-list contract, Revit-side schema/entity write and four-part read-back discipline, error-catalogue rows 21a-21c, Result-code policy note, and placement-record schema bump (version 1 to 2) are recorded in `docs/architecture/revit-extensible-storage-provenance.md`, whose manual evidence plan carries 2026-09-23 Revit 2027 session evidence for Steps 9-13; the first end-to-end run against the shipped build found and fixed a schema defect before any of Steps 9-13 could pass (`metersPerOutputUnit` needed a Number spec, not no spec at all, commit `e80a78c`). Issue #16's Revit 2027 evidence is complete as of 2026-09-23. Issue #19 designed and shipped the final ribbon icon pair on 2026-09-23 to 2026-09-24 (commit `e8eb33f` plus this closeout commit) following owner decision 5's "ship an icon for `CreateToposolidCommand` in the initial milestone" and the ribbon-and-command bullet below; its design lineage, theme-robustness decision, PNG-format contract, and manual evidence plan are recorded in `docs/architecture/revit-ribbon-icons.md`, whose Evidence section carries a 2026-09-24 Revit 2027 session finding both sizes pixel-exact in both ribbon themes. Issue #19's Revit 2027 evidence is complete as of 2026-09-24; the owner was shown the final pair on 2026-09-24 and requested no change. The agent must follow these rules for `SolidGround.Revit`:

- Layout: `src/SolidGround.Revit/SolidGround.Revit.csproj` inherits the root `Directory.Build.props`. `SolidGroundApplication.cs` implements `IExternalApplication` directly, with no static `Instance`. `Commands/CreateToposolidCommand.cs` is `sealed`. Its only `ProjectReference` is to `SolidGround.Core`. A `SolidGround.Tests` assembly-reference test must assert `SolidGround.Core.dll` never references `RevitAPI`, `RevitAPIUI`, or `SolidGround.Revit`.
- TFM and references: `net10.0-windows`. Local and deploy builds reference `RevitAPI.dll`/`RevitAPIUI.dll` through a `RevitInstallDir` MSBuild property (default `C:\Program Files\Autodesk\Revit 2027`, overridable), `HintPath`, `Private=false`, never committed. `UseWPF` is set only once a WPF surface exists. `CopyLocalLockFileAssemblies` is `true` from the project's creation.
- Manifest and context: one `.addin` manifest with one `Application` entry (Name "SolidGround", Assembly "SolidGround.Revit.dll", FullClassName "SolidGround.Revit.SolidGroundApplication", a freshly minted pinned `AddInId`, VendorId "SolidGround", VendorDescription "SolidGround"). `UseRevitContext` is `false`, `ContextName` is "SolidGround", `UseAllContextsForDependencyResolution` is `false` set explicitly. No `PublicAssemblies` or `Dependencies` unless a reviewed integration needs them. No temporary all-users manifest.
- Ribbon and command: one tab "SolidGround" (owner decision 8, 2026-09-20: the dedicated tab is kept with Autodesk's Add-Ins-tab guideline in view; see the conventions note), one panel, one `PushButtonData` for `CreateToposolidCommand`, shipped with an icon in the initial milestone. The tooltip is full-sentence prose. No `IExternalCommandAvailability`; a read-only Preflight runs before any transaction and returns `Result.Cancelled` on rejection. `[Transaction(TransactionMode.Manual)]` plus `[Regeneration(RegenerationOption.Manual)]` is fixed by convention. The top-level catch filter excludes `OutOfMemoryException` and `StackOverflowException`.
- Settings and logging: the settings file and logs are machine-wide, under `%ProgramData%\SolidGround\Revit\` (a settings file and a `Logs\` folder); the add-in itself still installs per-user. Settings use `System.Text.Json`, strict fail-loud decoding, and a mutex plus digest-conflict check plus atomic write. Logging uses `System.Diagnostics.Trace` with a real file `TraceListener`. Query strings stay redacted everywhere, never only in a subsystem. Diagnostics must never throw back into Revit. User-facing failures get one native `TaskDialog`; a long list is capped in the dialog and written in full to the log folder.
- Deployment and signing: the initial milestone ships a deploy script, not a by-hand copy loop (`scripts/Deploy-RevitAddIn.ps1`, Issue #14). The default per-user target is `%APPDATA%\Autodesk\Revit\Addins\2027` (confirmed against the installed product at implementation time). The script refuses to run while `Revit.exe` is running, enumerates the full dependency closure and fails closed on any missing file, hash-verifies every file, and publishes by atomic versioned-folder rename plus atomic manifest replace, keeping the previous two versions for rollback. Any all-user path is read at runtime from `ControlledApplication.AllUsersAddinsLocation` or `Application.AllUsersAddinsLocation` as described under "Revit 2027 rules", never hardcoded. Issue #17 (begun 2026-09-24) added self-signed Authenticode signing: a non-exportable `Cert:\CurrentUser\My` code-signing certificate (`CN=SolidGround Revit Add-in Signing`), minted by `scripts/Sign-RevitAddIn.ps1 -NewCertificate` and pinned in the committed, non-secret `scripts/signing-certificate.json` (no `.pfx` or password exists anywhere), signs `SolidGround.Revit.dll`, `SolidGround.Core.dll`, and every first-party release/install script via `scripts/Sign-RevitAddIn.ps1`'s default mode. `scripts/New-ReleasePackage.ps1` signs its own staged copies before zipping them; a developer may also run `scripts/Sign-RevitAddIn.ps1` against a local build before running `scripts/Deploy-RevitAddIn.ps1` in the ordinary dev loop. `scripts/Import-SigningTrust.ps1` performs a one-time, elevated, per-workstation import into `Cert:\LocalMachine\Root` and `Cert:\LocalMachine\TrustedPublisher`; without it, Revit still shows a signed-but-not-yet-trusted-publisher prompt (two choices, no "Load Once"), and a build that was never signed at all still shows the original unsigned three-choice prompt. See `docs/architecture/revit-release-packaging-and-signing.md`. This mechanism has not yet been exercised in a live Revit 2027 session.
- Debugging: the dev loop is build, deploy, restart Revit, verify by hash. No pyRevit, Python, IronPython, MCP live-driving harness, RevitLookup, or Add-In Manager dependency.
- Packaging: Issue #17 (begun 2026-09-24) adopted a formal scheme: a single `<Version>0.1.0</Version>` in `Directory.Build.props`, bumped by hand per release, whose informational version already carries the full git commit SHA through the .NET SDK's own automatic source-revision embedding (for example `0.1.0+11195a1ae30b8ac4405aa9bacbfd0392ab5c32ca`). `scripts/New-ReleasePackage.ps1` builds, signs, and stages a versioned, hashed release zip into `artifacts/release/`, a subfolder of the pre-existing, already git-ignored `artifacts/` bucket (`.gitignore:4`); no new release-staging folder was added. `scripts/Install-SolidGround.ps1`/`scripts/install.cmd` install it and `scripts/Uninstall-SolidGround.ps1` removes it. See `docs/architecture/revit-release-packaging-and-signing.md`.
- CI reference assemblies: see "Build and CI" and "Dependency policy" below.

## Provenance decision

The agent must use Extensible Storage for SolidGround-owned provenance on the created toposolid unless new 2027 evidence changes the tradeoff. Revit 2027 Extended Properties are built around `Autodesk.Revit.ExternalData.ExtendedPropertiesLink`, external-server load content, link data, bindings, and externally authored values. That lifecycle is useful for linked external property providers but unnecessary for metadata created and owned with one element. Revit 2027 Extensible Storage directly supports a schema-backed `Entity` attached to an `Element`, retrieved with `Element.GetEntity()` and removed with `Element.DeleteEntity()`.

The Extensible Storage schema must have a stable GUID and explicit schema version. It must preserve, at minimum, source dataset, collection date, quality level, horizontal datum, vertical datum, original and retained point counts, elevation minimum and maximum, output unit and foot definition, the complete local-origin offset needed to reverse the transform, and the `SolidGround.Revit` assembly's build identity: informational version, MVID, and SHA-256. The field-list contract must live in `SolidGround.Core`; the `SchemaBuilder` call that consumes it belongs in `SolidGround.Revit`. The schema must grant `AccessLevel.Public` read and `AccessLevel.Vendor` write. See `docs/architecture/revit-add-in-conventions.md` for the full field list and its basis in the owner's other Revit add-in.

## Data and numeric contracts

The first source is OpenTopography's `GET /API/usgsdem` endpoint with `datasetName=USGS1m`. The OpenAPI definition checked on 2026-09-15 still lists `AAIGrid`; the agent must request it explicitly because its default is `GTiff`. A bare AAIGrid response is followed by a second `GTiff` request for the identical box, read only for its GeoKeys, so such an acquisition costs two API calls against the key's daily quota, while a response that already carries a `.prj` or `.aux.xml` sidecar keeps the single-request path. The same definition says USGS 1 m access is restricted to academic users or an enterprise API key. The agent must surface authorization failures accurately and must not silently fall back to a coarser dataset.

The agent must support these AOIs: WGS 84 bounding box, latitude/longitude plus radius, and parcel polygon supplied as GeoJSON or WKT. Polygon clipping must support a configurable buffer. The agent must treat every AAIGrid `NODATA_value` as missing data before conversion, interpolation, clipping, statistics, or export. A sentinel must never become an elevation.

When OpenTopography returns a bare AAIGrid body with no reference metadata (the observed USGS 1 m behaviour), the agent must obtain the horizontal reference from the GeoTIFF GeoKeys returned for an identical second request, must verify exact grid agreement between the two responses, and must fail rather than assume when the GeoKeys are missing, unsupported, or disagree. Neither response carries a vertical reference; the agent must declare it from the dataset's published documentation (NAVD88 height in metres for USGS 1 m) and must label the origin of every horizontal and vertical reference in provenance, sidecars, and exports. The agent must not assume that the USGS 1 m mosaic uses the native coordinate system of a named source collection.

All transformations must be explicit and reversible. The agent must centralize length conversion in one tested component. Supported output units must state the exact definition. The default is U.S. survey foot, exactly `1200 / 3937` meters per foot. International foot, exactly `0.3048` meters, may be selected explicitly. Revit consumes decimal-foot coordinates; the local-origin and unit metadata must make the numeric interpretation unambiguous.

The agent must subtract a chosen projected local origin before coordinates enter Revit and must carry the original offset, CRS, datum, and unit through exports and provenance. The agent must reject any workflow that cannot reconstruct source coordinates from local coordinates plus metadata.

The default point budget is approximately 15,000. The agent must provide a terrain-aware simplifier, such as curvature-aware selection or TIN error simplification, and may also provide a clearly labeled simple sampler for comparison. The agent must not make every-Nth sampling the only or default algorithm. Tests must cover ridges, swales, edges, NODATA holes, determinism, and budget enforcement.

## Dependency policy

The agent must pin every package version. The agent must prefer the standard library for small, well-bounded functions and must document why each package is needed when it is added.

ProjNet is acceptable in Phase 1 for managed horizontal coordinate transformations after its exact API and supported definitions are verified. It does not by itself justify claims about vertical datum transformations. NetTopologySuite is acceptable because robust GeoJSON/WKT parsing, polygon buffering, holes, multipolygons, and clipping are substantive geometry operations; the agent must avoid adding it if the accepted Phase 1 scope narrows to a simpler geometry contract. Neither package is part of Phase 0.

The agent must not add a package that loads native binaries into the Revit process. The agent must not commit Autodesk Revit assemblies. A later Revit project may reference the locally installed 2027 SDK or a reviewed reference-assembly source, following the user's add-in conventions.

A shipped release must carry a `THIRD-PARTY-NOTICES` file naming every bundled third-party package's license and reproducing its required notice text; `scripts/New-ReleasePackage.ps1` fails closed if it is missing or does not name every third-party (non-`Project`-type) package the shipped `packages.lock.json` lists, together with its exact resolved version.

The `Nice3point.Revit.Api.RevitAPI` and `Nice3point.Revit.Api.RevitAPIUI` packages are acceptable, CI-only, exact-pinned to one 2027.x release, because they let the dedicated self-hosted Linux runner compile-check `SolidGround.Revit` without a licensed Revit install. Their MIT license (Copyright (c) 2022 Nice3point) covers only the packaging; Autodesk's SDK terms for the redistributed API surface are accepted by the owner for CI compile-checking only, matching the same ruling already applied to the owner's other Revit add-in. The agent must never reference these packages from a local build and must never let the shipped add-in depend on them.

## Secrets, downloads, and logs

The OpenTopography key must come from `OPENTOPOGRAPHY_API_KEY` in the process environment or .NET user secrets. `.env` files are local conveniences only and are ignored. The agent must never print, persist, commit, embed in a fixture, place in a URL shown in logs, or include the key in an exception message. The agent must redact request query strings before logging.

Downloaded rasters and generated bulk data are ignored by default. Small deterministic fixtures may be committed only after inspection proves that they contain no key, authorization header, signed URL, or personal token.

No personal information — addresses, home or client locations, names, contact details, user paths, or private infrastructure identifiers — may ever be committed, used in fixtures, commit messages, issues, or CI output. This repository is public. Examples must use the public example site or clearly labeled synthetic data, never a real, identifiable person's or client's location.

## Accuracy and product claims

The agent must describe SolidGround as a site-form tool, not a survey instrument. QL2 bare-earth data is roughly 10 cm vertical RMSE in favorable conditions and can be worse under mature canopy. Sparse ground returns and interpolation artifacts at the original test fixture are expected quality limitations and must remain visible in diagnostics and documentation. The agent must never claim suitability for foundation-perimeter grading; that work requires site measurement such as a rotary laser or professional survey.

## Test fixture and verification

SolidGround's product goal is to create an accurate, simplified toposolid for any property, quickly and easily — not just one lot. The primary offline test scenario is accordingly a generic "example site": centered at `41.591194, -93.603806` (NAD83 / UTM zone 15N, EPSG:26915; confirmed USGS 1 m DEM coverage), a small ~1,600 m² (~17,222 sq ft) example area on public, non-residential land. No street address, lot, plat, ZIP, or place name is ever recorded for it in this repository. The agent must keep default tests offline and deterministic. A real OpenTopography fetch must require an explicit opt-in flag and the environment key; lack of a key must skip or clearly decline the online test, not fail the offline suite.

The agent must add tests for every observable Core behavior, especially header validation, row/column counts, NODATA, coordinate orientation, units, local-origin reversibility, clipping, decimation, and deterministic export. The agent must not require Revit to run Core tests.

## Build and CI

The supported commands from the repository root are:

```powershell
dotnet restore SolidGround.slnx --locked-mode
dotnet build SolidGround.slnx --configuration Release --no-restore
dotnet test --project tests/SolidGround.Tests/SolidGround.Tests.csproj --configuration Release --no-build
```

The agent must keep `packages.lock.json` files current and committed when package references change. The agent must run restore, build, and tests before presenting a code change as complete.

GitHub Actions must remain ordinary .NET CI with no AI service. During Phase 1 it builds Core and CLI and runs offline tests on a repository-scoped, ephemeral self-hosted runner. Because the repository is public, this self-hosted workflow must accept trusted pushes to `main` only: no pull-request trigger, no fork code, and no automatic GitHub-hosted or secondary self-hosted fallback. It must retain its repository, owner, event, ref, and runner-identity guards and must not use GitHub-hosted cache or artifact storage.

This self-hosted runner lane was provisioned and audited under the owner's own infrastructure security review, which named this repository for a public-repository trusted-push exception. That review also recorded the owner's 2026-09-16 decision to harden this repository's own GitHub Actions settings to satisfy condition 9 below. Any change to the conditions below is a new security decision, not a routine workflow edit, and the agent must keep each one true:

1. The agent must trigger the workflow only on `push` to `main` in the canonical repository.
2. The agent must not add a pull-request, fork, issue, comment, release, external-dispatch, or reusable-workflow entry path.
3. The agent must keep a job-level guard checking the canonical repository, owner, push event, and `refs/heads/main` before runner allocation.
4. The agent must keep a first step checking those same fields plus the self-hosted runner's expected name pattern before checkout.
5. The agent must assume the GitHub App is installed on this repository alone, with Administration read/write and Metadata read only, no webhook, user permissions, or subscribed events.
6. The agent must assume the runner pod is unprivileged and runner-only, with no Docker daemon or service container.
7. The agent must not add repository secrets, deployment credentials, GitHub-hosted cache, or artifact storage to the job.
8. The agent must not add a GitHub-hosted or secondary self-hosted fallback.
9. The agent must keep the repository's GitHub Actions settings at full-commit-SHA pinning required and an allow-list narrowed to exactly the actions the workflow uses, with fork pull-request contributor approval tightened beyond GitHub's stock default.

The agent must also keep the owner's infrastructure runbook's never-list true here: never let this lane see a pull-request, fork, issue, comment, release, or external-dispatch event; never add a GitHub-hosted or secondary self-hosted fallback; never give the job repository secrets, deployment credentials, hosted cache, or artifact storage beyond the default read-only `GITHUB_TOKEN`; never treat the self-hosted runner label itself as a security boundary, and never assume the exception granted to this repository extends to any other repository; never let untrusted or fork code reach the runner under any label; and never perform the owner's other infrastructure changes (GitHub App, Kubernetes Secret, Helm release, or other runner-host state) from within this repository, since that is the owner's own infrastructure-session work under its own protocol, not SolidGround's.

Condition 9 is implemented today as: `sha_pinning_required=true`; `allowed_actions` narrowed to exactly `actions/checkout` and `actions/setup-dotnet`; fork pull-request contributor approval required for all outside collaborators; and the default workflow token permission read-only. If a run ever fails with an "action not allowed" message, the agent must report it and must not widen the allow-list without the owner.

The current Linux self-hosted runner does not include Autodesk's Revit reference assemblies. The agent must not make Phase 1 CI depend on unofficial repackaged Autodesk binaries. Phase 2 CI is decided (Issue #12, 2026-09-20) and active (Issue #14, 2026-09-21): `SolidGround.Revit` now exists, and the existing self-hosted lane's restore and build steps pass `-p:UseRevitReferenceAssemblies=true`, the CI-only MSBuild condition that swaps `SolidGround.Revit`'s local `HintPath` references for the exact-pinned `Nice3point.Revit.Api.RevitAPI`/`RevitAPIUI` `2027.0.10` packages, restored with their own separate `packages.ci.lock.json` (scoped to the same condition via `NuGetLockFilePath`, so the default `packages.lock.json` never gains a Nice3point entry), with `EnableWindowsTargeting` scoped to the same condition. Neither local builds nor the shipped add-in may ever use this condition; local and deploy builds keep referencing the installed Revit 2027 SDK directly. This gate keeps every one of the nine conditions above true and every item on the never-list unchanged: no pull-request trigger, the job-level guard and first-step check carried over, and no new action added to the allow-list. The allow-list stays exactly `actions/checkout` and `actions/setup-dotnet`; `actions/cache` must not be added. See `docs/architecture/revit-add-in-conventions.md` for the original mechanism and licensing position and `docs/architecture/revit-add-in-host-scaffold.md` for the activated gate's full detail.

## Authoritative references

- [Autodesk Revit 2027 API changes](https://help.autodesk.com/view/RVT/2027/ENU/?guid=f7165618-24c9-4160-a7a4-09979fe4a981)
- [Autodesk Revit 2027 Extensible Storage guide](https://help.autodesk.com/view/RVT/2027/ENU/?guid=Revit_API_Revit_API_Developers_Guide_Advanced_Topics_Storing_Data_in_the_Revit_model_Extensible_Storage_html)
- [Autodesk Revit SDK downloads](https://aps.autodesk.com/developer/overview/revit-api)
- [OpenTopography API documentation](https://portal.opentopography.org/apidocs/)
- [OpenTopography OpenAPI definition](https://portal.opentopography.org/apidocs/openapi.json)
- [Groundit architecture reference](https://github.com/lewismconte/groundit)

The agent must prefer these primary sources and current package documentation. If current evidence conflicts with this file, the agent must stop the affected design, report the conflict, and update `AGENTS.md` only with the user's accepted direction.
